# CLI & UX
typer[all]
rich

# config / env / data
python-dotenv
pyyaml

# networking / HTTP libs used from Python
httpx
requests

# Shodan API (python library + CLI)
shodan

# small utilities
tqdm
python-magic
